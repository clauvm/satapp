<ion-view view-title="{{user.firstName}}" ng-init="findOne()">
 <ion-nav-buttons side="right">
      <a class="button" href="#/app/users/{{user._id}}/edit">
        Editar
      </a>
      <a class="button" href="#" ng-click="delete()">
        Eliminar
      </a>
    </ion-nav-buttons>
  <ion-content class="padding">
  <h1>{{user.firstName}}{{user.lastName}}</h1>

    <p>
       {{user.firstName}}
          {{user.lastName}}
           {{user.username}}
            {{user.email}}
            {{user.created}}

    </p>
    
  </ion-content>
</ion-view>